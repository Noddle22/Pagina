function showContactInfo() {
    const contactInfo = document.getElementById("contactInfo");
    // Muestra u oculta la sección de información de contacto
    contactInfo.style.display = contactInfo.style.display === "none" ? "block" : "none";
}